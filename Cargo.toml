[package]
edition = "2018"

# Has the same name as the real divvunspell. This is just the Python wrapper.
publish = false 

[lib]
name = "divvunspell"
crate-type = ["cdylib"]

[dependencies.pyo3]
version = "0.13.2"
features = ["extension-module"]
